# requirements.txt

# Web Framework & API
fastapi[all]
uvicorn
python-dotenv
python-multipart

# Dashboard & UI
streamlit
pandas
plotly
psutil
streamlit-lottie
streamlit-agraph==0.0.45

# AI & Intelligence Layer
google-generativeai

# Networking & Scraping
requests
httpx
beautifulsoup4
validators
urllib3
playwright

# PDF Report Generation
reportlab

# Database & Queue
supabase
redis
docker
# Crypto (FIX: added — oast.py no longer uses this, but kept for other crypto needs)
# pycryptodome  ← removed from oast.py; add back if needed elsewhere

# NOTE: apktool is a Java binary installed via Dockerfile, not pip.
