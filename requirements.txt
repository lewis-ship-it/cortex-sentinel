
# Web Framework & API
fastapi[all]
uvicorn
python-dotenv
python-multipart

# Dashboard & UI
pandas
plotly
psutil
fastapi[all]
jinja2
python-multipart
pydantic>=2.0
aiofiles
# Networking & Scraping
requests
httpx>=0.27.0
beautifulsoup4
validators
urllib3
playwright

# Resilience / Retry
# tenacity provides production-grade exponential backoff used by:
#   core/http_client.py  — HTTP request retries with 429/Retry-After awareness
#   task_queue/redis_client.py — job re-queue back-off
tenacity>=8.2.0

# PDF Report Generation
reportlab

# Security & Authentication
bcrypt

# Database & Queue

docker
redis

# NOTE: apktool is a Java binary installed via Dockerfile, not pip.
# NOTE: google-generativeai has been permanently removed.
#       All AI inference is performed by the local Ollama container
#       (http://ollama:11434/api/generate) via httpx.

# Added by fix pass
lxml               # XML parsing for XXE payload construction
PyJWT>=2.8.0       # JWT decoding for JWT attack detection
cryptography       # Required by PyJWT for RS/ES algorithms